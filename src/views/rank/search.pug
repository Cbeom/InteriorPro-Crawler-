extends ../base
include ../mixins/rank
block content
    form(method="GET")
        input(type="text",name="title",placeholder="Search Title")
        input(type="submit", value="Search")
    hr
    if rank
        each item in rank
            +rank(item)